// def main(private field lon, private field lat, private field lon2, private field lat2, field n) {
//     assert( ( (lon-lon2)**2 + (lat-lat2)**2  )**(1/2)  < n);
//     return;
// }

// elec is the capacity of the owner of the car
// n is the capacity of the station

def main(private field elec, field n){
    assert(elec > n);
    return;
}